name: 📦 Build Windows Release ZIP

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      version:
        description: 'Version (e.g. 1.5.0)'
        required: true
        default: '1.5.0'

jobs:
  build-zip:
    name: Build Windows Package
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set version
        id: version
        shell: pwsh
        run: |
          if ("${{ github.event_name }}" -eq "workflow_dispatch") {
            $v = "${{ github.event.inputs.version }}"
          } else {
            $v = "${{ github.ref_name }}" -replace '^v', ''
          }
          echo "VERSION=$v" >> $env:GITHUB_OUTPUT
          echo "Version: $v"

      - name: Build Release ZIP
        shell: pwsh
        run: |
          $version = "${{ steps.version.outputs.VERSION }}"
          .\scripts\build-release.ps1 -Version $version -Output "$env:GITHUB_WORKSPACE\dist"

      - name: Upload Windows ZIP
        uses: actions/upload-artifact@v7
        with:
          name: SignageCMS-Windows-v${{ steps.version.outputs.VERSION }}
          path: dist/SignageCMS-v*.zip
          retention-days: 90

      - name: Package info
        shell: pwsh
        run: |
          Get-ChildItem "dist\*.zip" | ForEach-Object {
            $mb = [math]::Round($_.Length / 1MB, 2)
            Write-Host "📦 $($_.Name)  —  $mb MB"
          }
