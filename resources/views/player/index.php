<?php
/**
 * Hotel Media — پخش‌کننده‌ی پشتیبان
 *
 * وقتی فایل پروفایلِ انتخاب‌شده وجود نداشته باشد، PlayerController
 * این را include می‌کند.
 *
 * تا پیش از این، اینجا یک نسخه‌ی کامل و جداگانه از پخش‌کننده بود
 * (۹۱۰ خط) که با profiles/modern.php هم‌پوشانی داشت ولی از آن جدا
 * تکامل پیدا کرده بود. نتیجه: قابلیت «پیام‌های زمان‌بندی‌شده» فقط
 * اینجا پیاده شده بود و روی تابلوهای واقعی — که همیشه پروفایل
 * modern را می‌گیرند — هیچ‌وقت نشان داده نمی‌شد. آن قابلیت به
 * modern.php منتقل شد و این فایل حالا فقط به آن ارجاع می‌دهد تا
 * دوباره از هم دور نیفتند.
 *
 * متغیرهای $screen و $playlist از PlayerController می‌آیند و در
 * همین دامنه برای فایل زیر در دسترس‌اند.
 */
require __DIR__ . '/profiles/modern.php';
